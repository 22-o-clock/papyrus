# AGENTS

- コミットメッセージには Conventional Commits を使用してください（例：`feat:`, `fix:`, `docs:`）。
- 変更箇所は、ユーザーの指示の範囲内のものとし、また、必要最小限に抑えてください。
- コードには Google style の docstring を含めてください。ただし、単純な処理については、引数、戻り値、例外の説明を省略できます。
- ドキュメントやツールの表現は、可能な限り、実装に依存しないものにしてください。
- コミット前に以下のリンターを実行してください。失敗した場合には、必要な修正を行ってください。
  - uv run ruff check .
  - uv run ruff format --check .
  - uv run ty check .

## Pull Requests

### 作成前

- 今回の修正箇所を`/docs`に反映する必要があるかを確認します。必要であれば、修正を反映するコミットを行います。

### マージ前

- マージを行う前に、以下の全ての点を確認してください。
  - コードの変更を行った場合、デバッグ設定でボットを起動した上で、(a)ボットが問題なく起動すること、(b)目的の更新が反映されていること（可能な限り）について、ユーザーの確認を得たこと。詳細については`docs/development.md`を参照してください。
  - ローカルで`.env`に含まれている環境変数のうち、本番実行に必要となる変数が、全てGitHub ActionsのSecretsに登録されていることを確認したこと。登録されていない場合、マージは行わず、ユーザーにまずSecretsを追加するよう警告してください。
  - GitHub上で、全てのcheckの通過を確認したこと。
