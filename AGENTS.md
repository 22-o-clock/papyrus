# 開発ルール

## 適用範囲

- このルールは、新規コードと今後変更するコードに適用する。
- 関係のない既存コードを、ルール適合だけを目的として一括修正しない。

## Pythonコード

- すべての関数とメソッドに、引数と戻り値の型ヒントを付ける。内部用も対象とする。
- 識別子には英語を使う。
- コメントとdocstringには日本語を使う。利用者向けメッセージとログは既存の用途・言語に合わせる。
- docstringは、名前と型だけでは目的、制約、副作用が分からない関数やクラスに付ける。
- コメントには、コードから読み取れる処理内容ではなく、判断の理由、制約、意図を書く。
- `# noqa`、`# type: ignore`、lintルールの除外は安易に追加しない。必要な場合は対象を局所化し、具体的なエラーコードと理由を記載する。

## テスト

- 挙動を変更する場合は、可能な限り利用者から見える挙動を保証する回帰テストを追加する。
- 設定変更、単純な配線、外部サービスとの薄い連携などでテストの費用対効果が低い場合は省略できる。その場合は作業報告に理由を書く。

## 完了時の確認

変更した範囲に対して、次の確認を実行する。

```powershell
uv run ruff check <変更対象>
uv run ruff format --check <変更対象>
uv run ty check <変更対象>
```

- 対象を限定できない変更では、`<変更対象>` を `.` に置き換えて全体を確認する。
- 既存コード由来の違反で全体確認が失敗する場合は、変更範囲の確認結果と既存違反を区別して報告する。
- CIとpre-commitは現時点では必須としない。

## バージョン管理

- 意味のある変更単位が完成し、必要な確認を終えた時点で適宜コミットする。
- コミットには、その変更単位に関係するファイルだけを含める。
